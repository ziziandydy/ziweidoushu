# 🚀 Vercel 部署注意事項

## ✅ 已修復的問題

### 1. pricing.html 404 錯誤

**問題：** `/pricing.html` 頁面顯示 404 錯誤

**解決方案：**
- ✅ 已創建 `public/pricing.html` 價格頁面
- ✅ 已在 `vercel.json` 中添加路由配置

**修改檔案：**
- `public/pricing.html` (新建)
- `vercel.json` (新增路由)

---

## ⚠️ Node.js 版本警告

### 警告訊息

```
Warning: Due to "engines": { "node": ">=16.0.0" } in your `package.json` file, 
the Node.js Version defined in your Project Settings ("22.x") will not apply, 
Node.js Version "24.x" will be used instead.
```

### 說明

這個警告**不會影響功能**，只是提醒您：

1. **當前狀況：**
   - `package.json` 中指定：`"engines": { "node": ">=16.0.0" }`
   - Vercel 專案設置：Node.js 22.x
   - **實際使用：** Node.js 24.x（自動升級到最新穩定版本）

2. **為什麼會這樣？**
   - `>=16.0.0` 表示「至少 16.0.0 或更高版本」
   - Vercel 檢測到此設定後，會自動使用符合要求的最新版本（24.x）
   - 這確保了您的專案使用最新的 Node.js 功能和安全更新

3. **影響：**
   - ✅ **功能正常**：所有 API 和功能都正常運作
   - ✅ **性能更好**：使用最新的 Node.js 版本可能性能更佳
   - ⚠️ **只是警告**：不影響部署和運行

---

## 🔧 可選修復方案

如果您想要消除警告訊息，有兩種選擇：

### 選項 1：更新 package.json（推薦）

將 Node.js 版本範圍更新為更具體的版本：

```json
{
  "engines": {
    "node": ">=18.0.0"
  }
}
```

或固定使用 22.x（與 Vercel 專案設置一致）：

```json
{
  "engines": {
    "node": "22.x"
  }
}
```

### 選項 2：移除 engines 欄位（不推薦）

如果移除 `engines` 欄位，Vercel 會完全使用專案設置中指定的版本（22.x），但這樣會失去版本控制的明確性。

---

## 📝 建議

**建議：保持現狀或使用選項 1**

1. **保持現狀**：警告不影響功能，使用最新版本可能更安全
2. **更新為 22.x**：如果希望與 Vercel 專案設置完全一致，可以明確指定
3. **更新為 >=18.0.0**：如果不需要最新版本，可以限制在 LTS 版本

---

## 🔍 檢查 Node.js 版本

要查看當前使用的 Node.js 版本，可以在任何 API 中添加：

```javascript
console.log('Node.js version:', process.version);
```

或在 Vercel Dashboard 的部署日誌中查看。

---

## 📚 相關資源

- [Vercel Node.js 版本文件](https://vercel.com/docs/concepts/functions/serverless-functions/runtimes/node-js)
- [package.json engines 說明](https://docs.npmjs.com/cli/v9/configuring-npm/package-json#engines)

---

**最後更新：** 2025-01-XX


